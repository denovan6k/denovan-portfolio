"use client"

// No-op cursor component to satisfy imports without adding any animations or DOM elements.
// Keeps the UI clean while avoiding import errors across pages.
export default function CustomCursor() {
  return null
}
